@~/.config/opencode/AGENTS.md

# Claude Code Compatibility

OpenCode owns the global behavioral policy and reusable skills. When an
imported instruction names an OpenCode-only skill, agent, or tool that Claude
Code does not expose, follow the underlying policy with Claude Code's native
capabilities. Do not invoke the OpenCode CLI merely to reproduce its harness
internals.
